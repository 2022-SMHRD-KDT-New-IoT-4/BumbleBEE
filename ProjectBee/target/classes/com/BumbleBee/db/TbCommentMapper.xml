<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.BumbleBee.model.TbCommentDAO" > 

 <insert id="commentwrite" parameterType="TbCommentDTO">
 	INSERT INTO tb_comment ( cmt_content,  mb_id, cmt_shares) VALUES (#{cmtContent}, #{id}, 1); 
 </insert>
  
  <update id="commentmodify" parameterType="TbCommentDTO">
  	update tb_comment
  	set cmt_content = #{}
  	where mb_id = #{id} and board_seq = #{boardSeq} and cmt_seq = #{cmtSeq}
  </update>
  
</mapper>